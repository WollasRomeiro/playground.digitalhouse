/*Desenvolva uma função em JavaScript chamada menorNumero que recebe dois parâmetros, numeroA e numeroB, e retorna 
o menor entre os dois números. */


function menorNumero(numeroA, numeroB){
    if (numeroA > numeroB) {
        return numeroB;
    }else {
        return numeroA;
    }
}